# Keep standalone CI and checkouts nested under HyperView on the same rules.
line-length = 100
target-version = "py310"

[lint]
select = ["E", "F", "I", "N", "W", "UP"]
ignore = ["E501"]

[lint.isort]
known-first-party = ["hyperview"]
